# Figma API token (required)
# Generate at: https://www.figma.com/developers/access-tokens
# Scope needed: file_content:read
FIGMA_API_TOKEN=figd_your_token_here

# Optional MCP source settings
# When set, --source mcp can call this endpoint for diagram payload retrieval.
JAMAID_MCP_ENDPOINT_URL=

# Optional bearer token sent to MCP endpoint.
JAMAID_MCP_AUTH_TOKEN=

# Optional MCP request timeout in milliseconds (default: 10000)
JAMAID_MCP_TIMEOUT_MS=10000
